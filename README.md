# llmdiff

A tool for identifying behavioral divergences between LLMs.

## Project Structure

- `llmdiff/`: Core library
- `app.py`: Gradio UI
- `data/`: Pre-computed fingerprints
- `pyproject.toml`: Project configuration
